#include "main.h"
#include <stdlib.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @n: inserted value
 * Return: Returns abs for iserted int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else 
	{
		return (-n);
	}
}

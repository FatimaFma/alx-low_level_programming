#include "main.h"
/**
 * add - adds two integers and returns the result
 * @num1: inserted number1
 * @num2: inserted number2
 * Return: the result of summation
 */
int add(int num1, int num2)
{
	int result;

	result = num1 + num2;
	return (result);
}
